# Environment Configuration Template
# Copy this file to .env and update with your values

# ==================================================
# LOCAL DEVELOPMENT (Running on your PC)
# ==================================================

# For local MongoDB (default port 27017):
MONGODB_URI=mongodb://localhost:27017/wvp_consulting
MONGODB_DB_NAME=wvp_consulting

# For local MongoDB with authentication:
# MONGODB_URI=mongodb://username:password@localhost:27017/wvp_consulting?authSource=admin

# Session Secret (IMPORTANT: Change this in production!)
SESSION_SECRET=dev-secret-key-change-in-production

# Node Environment
NODE_ENV=development

# Server Port
PORT=5000

# ==================================================
# DOCKER DEPLOYMENT
# ==================================================

# MongoDB Configuration (for docker-compose)
MONGO_ROOT_USER=admin
MONGO_ROOT_PASSWORD=adminpassword
MONGO_PORT=27017

# Mongo Express (Web-based MongoDB Admin Interface)
MONGO_EXPRESS_PORT=8081
MONGO_EXPRESS_USER=admin
MONGO_EXPRESS_PASSWORD=pass

# Application Ports
APP_PORT=5000
DEV_PORT=3000

# ==================================================
# PRODUCTION / CLOUD MONGODB
# ==================================================

# For MongoDB Atlas or other cloud MongoDB:
# MONGODB_URI=mongodb+srv://username:password@cluster.mongodb.net/wvp_consulting?retryWrites=true&w=majority

# ==================================================
# NOTES
# ==================================================
# - Admin users and logs are stored in MongoDB
# - Blog posts are stored in MongoDB
# - Other data (contacts, achievements, page content) uses in-memory storage
# - If MONGODB_URI is not set, the app will use in-memory storage for all data

# MongoDB Configuration
MONGO_ROOT_USER=admin
MONGO_ROOT_PASSWORD=adminpassword
MONGODB_DB_NAME=wvp_consulting
MONGO_PORT=27017

# Application Configuration
APP_PORT=5000
DEV_PORT=3000
NODE_ENV=production

# Session Secret (IMPORTANT: Change this in production!)
SESSION_SECRET=change-this-to-a-random-secret-key-in-production

# MongoDB Connection (Auto-generated from above variables)
# MONGODB_URI will be automatically set by docker-compose
# Format: mongodb://MONGO_ROOT_USER:MONGO_ROOT_PASSWORD@mongodb:27017/MONGODB_DB_NAME?authSource=admin

# Optional: External MongoDB (if not using Docker MongoDB)
# Uncomment and configure if you want to use an external MongoDB instance
# MONGODB_URI=mongodb://user:password@external-host:27017/database
# MONGODB_DB_NAME=wvp_consulting

# Note: The app uses in-memory storage for most data (users, contacts, achievements, etc.)
# Only blog posts are persisted to MongoDB. If MONGODB_URI is not set, the app will
# fall back to in-memory storage for blog posts as well.
